/**
 * Subscribes an email to ConvertKit.
 *
 * @param email The email to subscribe.
 * @param tags The tags to add to the subscriber.
 */
export async function subscribeToConvertKit(email: string, tags: string[]): Promise<void> {
  // TODO: Implement this by calling the ConvertKit API.
  console.log(`Subscribing ${email} to ConvertKit with tags: ${tags}`);
}
